# FlySF Environment Variables

# === 3D TILES AUTH (use ONE of these) ===

# Option 1: Multiple Google API Keys (RECOMMENDED for high traffic)
# Comma-separated keys from different Google Cloud projects
# Each user session randomly gets one key - distributes rate limits
# VITE_GOOGLE_API_KEYS=key1,key2,key3,key4

# Option 2: Cesium Ion (separate rate limits from Google)
# Get token from: https://ion.cesium.com/tokens
# VITE_CESIUM_ION_TOKEN=your_cesium_token_here

# Option 3: Single Google Maps API Key (direct)
# VITE_GOOGLE_MAPS_API_KEY=your_api_key_here

# === MULTIPLAYER ===

# WebSocket server URL (Fly.io multiplayer server)
# Production: wss://flysf-server.fly.dev
# Local dev:  ws://localhost:8080
VITE_WS_URL=wss://flysf-server.fly.dev
